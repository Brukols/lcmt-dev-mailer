# Dependencies, restored with `yarn install`.
node_modules/

# PHPUnit result cache.
.phpunit.cache/

# Build output stays tracked: WordPress installs the plugin by copying it,
# there is no build step on the server.

.DS_Store
